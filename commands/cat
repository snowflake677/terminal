import sys,terminal
sys.path.append("/")
def cat(arg):
    if arg == "-h":
        print("concatenate\ncat [file]")
    else:
        with open(arg, 'r') as fin:
            print(fin.read())
cat(terminal.args[0])