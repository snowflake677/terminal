import sys,terminal,requests
sys.path.append("/")
if terminal.args[0] == "-h":
    print("copys the source of a url to a file\nwget [url] [file]")
else:
    r = requests.get(terminal.args[0], allow_redirects=True)
    open(terminal.dir+"\\"+terminal.args[1], 'wb').write(r.content)