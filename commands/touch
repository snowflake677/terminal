import sys,terminal
sys.path.append("/")
def toutch(arg):
    if arg == "-h":
        print("creats a file\ntoutch [file name]")
    else:
        open(terminal.dir+"\\"+arg,"w+").close
toutch(terminal.args[0])
