import sys,terminal
sys.path.append("/")
def touch(arg):
    if arg == "-h":
        print("creats a file\ntouch [file name]")
    else:
        open(terminal.dir+"\\"+arg,"w+").close
touch(terminal.args[0])
