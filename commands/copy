import sys,terminal,shutil
sys.path.append("/")
def copy(arg1,arg2):
    if arg1 == "-h":
        print("copys a file\ncopy [source] [target]")
    else:
        shutil.copy2(arg1,arg2)
copy(terminal.args[0],terminal.args[1])